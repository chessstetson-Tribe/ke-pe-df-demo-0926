import { PlaceholderScreen } from "./PlaceholderScreen";

export function A1SearchScreen() {
  return (
    <PlaceholderScreen
      momentLabel="Moment A1 — Search"
      title="Natural-language document retrieval"
      note="The fallback path for when nothing auto-matched. Not yet built in this pass — A0's auto-matching is the primary flow."
    />
  );
}
