import { PlaceholderScreen } from "./PlaceholderScreen";

export function C1DiffScreen() {
  return (
    <PlaceholderScreen
      momentLabel="Moment C1 — Term sheet diff"
      title="Business issues vs. legal comments, triaged"
      note="Not yet built in this pass. Target shape once implemented: raw flags -> attorney-relevant -> client-shown, with a Partner view showing only the final list."
    />
  );
}
