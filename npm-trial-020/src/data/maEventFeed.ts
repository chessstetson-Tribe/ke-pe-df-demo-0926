import type { CrossPracticeEvent } from "@/state/types";

// Phase 2 (Moment E2). Modeled as a feed distinct from Debt Finance's own document
// set, per the brief's explicit data-model note — empty in phase 1.
export const MA_EVENT_FEED: CrossPracticeEvent[] = [];
